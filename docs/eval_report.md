# 检索质量评测报告（Recall@k）

> 生成日期：2026-04　|　评测脚本：`scripts/eval_retrieval.py`

---

## 1. 评测目标

量化验证检索链路的召回质量，对比**向量检索**与**混合检索（BM25 + 向量 + RRF）**在中文文档上的 Recall@k，为调参和算法选择提供数据依据。

## 2. 评测方法

- **数据集**：用户 1 知识库，4 份中文文档（刑法、劳动合同法、西游记、三国演义），共 7904 个 chunk。
- **评测集**：8 条「查询 → 相关文档文件名」标注对。
- **指标**：`Recall@5` = 前 5 个检索结果中命中相关文档的比例。
- **检索实现**：直接调用 FAISS `similarity_search_with_score`（向量）与 `hybrid_search`（混合），绕过高层 `retrieve_for_rag` 的 UI 耦合，聚焦检索本身。

## 3. 结果

| 检索模式 | 平均 Recall@5 | 单查平均耗时 |
|----------|--------------|-------------|
| 向量检索 | **1.0000** | ~14ms（热） |
| 混合检索 | **1.0000** | ~480ms（热）/ 31s（BM25 冷启动） |

### 3.1 逐条结果

| 查询 | 相关文档 | 向量命中 | 混合命中 |
|------|---------|---------|---------|
| 盗窃罪的量刑标准是什么 | 刑法 | ✅ | ✅ |
| 故意伤害罪如何处罚 | 刑法 | ✅ | ✅ |
| 劳动合同试用期最长多久 | 劳动合同法 | ✅ | ✅ |
| 解除劳动合同需要什么程序 | 劳动合同法 | ✅ | ✅ |
| 孙悟空大闹天宫的情节 | 西游记 | ✅ | ✅ |
| 唐僧师徒取经路上遇到哪些妖怪 | 西游记 | ✅ | ✅ |
| 关羽过五关斩六将 | 三国演义 | ✅ | ✅ |
| 诸葛亮草船借箭的故事 | 三国演义 | ✅ | ✅ |

## 4. 结论与发现

1. **检索质量达标**：在文档区分度高的中文标注集上，两种模式 Recall@5 均为 1.0，说明 bge-small-zh-v1.5 嵌入 + 分块策略对语义明确的查询召回良好。

2. **混合检索冷启动慢**：BM25 索引首次构建耗时约 31 秒（7904 chunk），之后缓存命中降到 ~480ms。**优化建议**：
   - 入库完成后**预构建并持久化 BM25 索引**（当前已有 `bm25_index.pkl` 持久化，但需确保入库流程主动调用 `rebuild_bm25_index`）；
   - 服务启动时**预热**向量库 + BM25 索引，避免首个用户首查卡顿。

3. **评测集偏小**：当前 8 条样本且文档区分度极高（法律 vs 小说），Recall 满分在预期内。**若要更有说服力**，需扩充：
   - 增加**同领域近似文档**（如多份法律条文），测试细粒度区分能力；
   - 增加**负样本**（query 无对应文档），测试是否错误召回；
   - 引入 **nDCG** 衡量排序质量，而不只是 Recall。

## 5. 扩充评测（含负样本 + nDCG + MRR）

针对上述第 3 点，已制作并入库 3 份**同领域近似 + 跨领域 + 负样本**评测语料（`eval_corpus/`，入库到 user 99）：

| 文档 | 类型 | 用途 |
|------|------|------|
| `治安管理处罚法节选_20260815.txt` | 法律（与刑法同领域近似） | 测试「盗窃」细粒度区分 |
| `劳动法节选_20260815.txt` | 法律（与劳动合同法同领域近似） | 测试「试用期/解约」区分 |
| `Python编程入门_20260815.md` | 技术（异领域） | 测跨领域区分 + 负样本干扰源 |

### 5.1 扩充评测结果（user 99，8 样本）

| 指标 | 向量检索 | 混合检索 |
|------|---------|---------|
| Recall@5（正样本 6 条） | **1.0000** | **1.0000** |
| nDCG@5 | 2.2113 | 2.1325 |
| MRR | 0.75 | 0.75 |

### 5.2 关键发现：负样本误召回（真实问题）

> 2 条负样本（「量子力学薛定谔方程」「红烧肉怎么做」）在两种模式下都**错误返回了 Python 文档**。

**根因**：知识库较小时，`similarity_search_with_score(query, k=5)` 无条件返回 top-5 最近邻，**缺少低分阈值兜底**。检索管线中的 `SIMILARITY_THRESHOLD`（0.3）过滤位于高层 `finalize_retrieval_from_scored`，而直接向量检索路径未做该过滤，导致无关查询也硬凑出「最近邻」。

**优化方向**：
1. 在检索出口统一施加 `SIMILARITY_THRESHOLD` 低分过滤，低于阈值即返回空（而非凑数）；
2. 或对「全部结果分数极低」的情况返回「未找到相关内容」，避免误导。

> 这一发现的价值：证明了「检索系统对无关查询的鲁棒性」是需要显式处理的，而非默认成立。

## 6. 复现方式

```bash
# 构建评测知识库（首次）
python scripts/build_eval_kb_20260815.py --user 99

# 评测：向量 + 混合，含 Recall/nDCG/MRR
python scripts/eval_retrieval.py --user 99 --k 5 --mode both
```
