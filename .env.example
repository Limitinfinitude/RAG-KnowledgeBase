# ===== RAG-KnowledgeBase 环境变量覆盖模板（可选）=====
# 密钥与模型默认配置统一存放在 config.json（启动读一次），请优先编辑 config.json：
#   cp config.example.json config.json   # 然后填写真实密钥
#
# 本 .env 仅用于「环境变量优先级最高」的临时覆盖场景，一般无需填写。
# 复制本文件为 .env 后填写（.env 已被 .gitignore 忽略）。
#
# 示例（临时覆盖，优先级高于 config.json）：
# MYSQL_PASSWORD=临时密码
# API_KEY=sk-临时覆盖
# SILICONFLOW_API_KEY=sk-临时硅基流动密钥

# --- 可选：部署相关 ---
# RAG_USER_PORT=4010
# RAG_ADMIN_PORT=4011
# RAG_BIND_HOST=0.0.0.0

# --- 可选：OCR ---
# Tesseract 可执行文件路径（默认自动探测 PATH 与常见安装位置）
# TESSERACT_CMD=/usr/bin/tesseract
